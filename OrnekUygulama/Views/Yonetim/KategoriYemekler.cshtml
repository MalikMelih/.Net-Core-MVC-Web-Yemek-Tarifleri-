@model IEnumerable<OrnekUygulama.Models.YemekTarifleri>

@{
    ViewData["Title"] = "KategoriYemekler";
    Layout = "~/Views/Shared/_yonetimtema.cshtml";
}

<h1>Kategori Yemekler</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                ID
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Yemekadi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sira)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Eklemetarihi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aktif)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kategori)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TarifId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Yemekadi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sira)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Eklemetarihi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Aktif)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kategori.Kategoriadi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TarifId">Düzenle</a> |
                <a asp-action="Details" asp-route-id="@item.TarifId">Görüntüle</a> |
                <a asp-action="Delete" asp-route-id="@item.TarifId">Sil</a>
            </td>
        </tr>
}
    </tbody>
</table>
